export enum Gender {
  Male = 1,
  Female = 2,
}

export enum Provider {
  System = 0,
  Google = 2,
}

export enum Role {
  User = 0,
  Admin = 1,
}

export enum TokenType {
  Access = 0,
  Refresh = 1,
}
